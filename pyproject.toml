[project]
name = "kalshi-weather-resolver-demo"
version = "0.1.0"
description = "Recruiter-safe weather market settlement resolver demo."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]

